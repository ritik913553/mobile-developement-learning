name: Preview Android Build

on:
  workflow_dispatch: {}

jobs:
  build_android:
    name: Build Android Preview
    type: build
    params:
      platform: android
      profile: preview


#  for run this worlflow u need to run command because ye manual trigger wala workflow hai
# npx eas workflows:run "workflow-file path"